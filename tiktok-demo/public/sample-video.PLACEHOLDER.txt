PLACEHOLDER — add the real sample video here.

Before deploying, place a short vertical MP4 at:

    tiktok-demo/public/sample-video.mp4

Requirements:
- 9:16 aspect ratio (TikTok vertical), e.g. 1080x1920
- 5–15 seconds, H.264 + AAC
- Any clip exported from the ContentBoss Studio pipeline works

The demo page (index.html) previews this file in Step 2 and uses its public URL
as the default for the Content Posting API PULL_FROM_URL flow. If the file is
missing, paste any publicly accessible HTTPS MP4 URL into the Video URL input
instead.

Delete this placeholder file once sample-video.mp4 is added.
